# dotfiles
🔧 .files

## Manual Installation
Create symbolic links for the configurations you want to use, e.g.:

```shell
ln -s ~/.dotfiles/.vimrc ~/.vimrc
```

## Using GNU Stow

```shell
cd ~/.dotfiles
stow vim
stow tmux
```
